package bhaymax.command;

/**
 * Provides enumeration values representing
 * the options that can be passed to the filter
 * command
 */
public enum FilterOpt {
    DATE_ON,
    DATE_BEFORE,
    DATE_AFTER,
    TIME_ON,
    TIME_BEFORE,
    TIME_AFTER;
}
